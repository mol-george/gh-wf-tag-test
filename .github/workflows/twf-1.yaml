name: twf1
on:
  push:
    branches: [ "main" ]
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: Is Push Coming From release/xx.xx.xx branch
        uses: sergeysova/jq-action@v2
        with:
          # cmd: jq '.' ${{ toJson(github) }}
          cmd: jq '.' ${{ github }}